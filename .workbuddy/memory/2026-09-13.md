# 2026-09-13 工作日志

## 站点创建（阶段 0–7 代码侧）

从母站仓库 `meihao-water` 导出干净副本，建成水邻居独立官网。

### 阶段 0 · 建仓
- 用 `git ls-files` 清单复制 66 个已跟踪文件（1.98MB）到 `C:\Users\shang\WorkBuddy\水邻居官网`，
  显式排除 35 个母站专属文件/目录（`.workbuddy/`、`public/brand/`、`public/company/`、
  `public/hero/years-*`、`src/data/brands.ts`、`docs/phase1|phase2`、`backfill.yml`、母站 favicon、
  母站微信校验文件、README/CHANGELOG）。
- `git init -b main` 重建全新历史（不继承母站变更记录）。
- 保留 `src/app/icon.svg`（通用水波图形，无品牌字样），弃用母站 `favicon.ico`。

### 阶段 1–5 · 换品牌
- **数据层**：重写 `site.ts`（加 `titleSuffix`、`parent` 背书、`logo` 资产配置三件套）、
  `company.ts`（加 `waterType` / `tagline` / `serviceArea`，删 `shuineighbor`）、
  `products.ts`（收敛为 18.9L 桶装水 + 10L 一次性桶装水）、`navigation.ts`（4 项）、
  `media.ts`（34 → 15 条，删母站专属，logo 留 null）；删除 `brands.ts`。
- **逻辑层**：`seo.ts` 的 SITE_SUFFIX 改为读 `site.titleSuffix`（去掉硬编码的母站措辞与「美好水业」判断）；
  `jsonld.ts` 重写（Organization/LocalBusiness 用品牌名 + legalName + `parentOrganization`，
  按 `site.logo.color` 决定是否输出 logo，移除未经验证的 `priceRange`）；`layout.tsx` 的
  title/OG/twitter 与 icons（改为 App Router 约定接管 icon.svg）。
- **页面**：新建 `BrandPoints.tsx`（三特性）、`SourceSection.tsx`（水源摘要）；
  删除 `YearsSection` / `ShuiNeighborSection`；重写 `/about` 与新增 `/source`；
  `/products` 改为图片卡片 + 服务段（删代理品牌 strip）；删除 `/brands` `/service` `/shuineighbor`；
  `sitemap.ts` 路由同步。
- **组件/样式**：`Logo.tsx` 重写为「读 `site.logo` + 无素材时渲染文字标降级」（不再写死 logo 路径与母站品牌名，
  宽高比改为 `height + width:auto` 以适配任意形状）；Footer/MobileMenu/Hero/CitySection/ClosingCTA 文案与背书；
  `globals.css` 品牌色从母站深海蓝改为水邻居蓝绿（`--brand` 改为 `#17786f`，`--brand-deep` 保留藏青）。
- 清理两处指向已删文档的注释（`motion.ts` / `Reveal.tsx` 里的 phase1 引用）。

### 阶段 7 · 部署配置
- `package.json`：name → `shuineighbor-water`、repository 改新仓、version 重置 1.0.0；
  `package-lock.json` 根 name/version 同步（离线直接改 JSON，避免 npm 联网）。
- 新增 `infra/shuineighbor-pull.sh`（5 个常量改为 shuineighbor 路径/仓库）；
  删除继承来的 `infra/mhsy-pull.sh` 与 `infra/Caddyfile`。
- **关键设计决定**：`/etc/caddy/Caddyfile` 是服务器级共享配置（同机还有母站块），
  因此归**母站仓库**维护，本仓库不含 Caddyfile、**删掉 ci-cd.yml 的 caddy job**，
  并在文件里写明原因与人工 apply 步骤。
- `.github` 改动：`ci-cd.yml` 去掉 caddy job + smoke test 改新域名（5 页 + 关键内容 + 母站背书 +
  **`矿泉水` 合规断言**）；`rollback.yml` 站点路径/域名/`accept-new`，并注明默认模式下应用服务器本地回滚；
  `deploy-site` action 的站点路径改为可传参 `site_path`（默认 `/var/www/shuineighbor/out`）。
- 全部 YAML 用 PyYAML 校验通过（ci-cd jobs: build/release/deploy/publish-dist）。
- 新增 `README.md`（含内容维护约定、Logo/图片替换方式、合规红线、素材待补清单）与 `CHANGELOG.md`（`[1.0.0]`）；
  `docs/deploy-lighthouse.md` 按新站重写（含 cron 安全追加、Caddyfile 归属、双站隔离验收、迁移清单）。

## 重大发现：物料库里已有完整品牌视觉系统（待用户确认后接入）

路径 `D:\剪辑素材\美好商贸\宣传物料\水邻居\`，是一套 **V1.0 / 2026** 的正式品牌规范：

| 资产 | 文件 | 说明 |
|---|---|---|
| Logo 主标 | `水邻居Logo概念1-邻里标签主标.png` | 1024×1024。**白底（非透明）**，手绘圆角标签 + 水滴 +「✓ 今日已送达」标签 |
| Logo 概念 4 | `水邻居Logo概念4-手写标注式.png` | 备选方案 |
| Logo 使用规范 | `水邻居Logo使用规范.png` | 有正式规范页 |
| 品牌视觉规范手册 | `水邻居品牌视觉规范手册.png` | 4551×2560，含 Brand Core / Logo System / **Color System** / Graphic Elements / Tag Language / Digital Application |
| 金色艺术字 | `水邻居金色艺术字.png` | 1763×892，**RGB 无透明**（白底金字）→ 可抠底做深底版 |
| 辅助图形系统 | `水邻居辅助图形系统大全.png` | 标签框架 / 图标符号（水滴·爱心·对钩·家庭·社区网点·配送…）/ 手绘标注 / 组合示例 |
| 桶标 | `水邻居18L/19L/6.5L 桶标`、`18L一次性桶装水桶标`、`水邻居10l围标转曲线源文件.ai` | 多条规格 |
| 应用展示 | 小程序首页/关于我们/下单页面高保真、微信封面图、社区海报A、社交媒体应用、数字场景应用 | 可直接参考版式 |

### ⚠️ 与既有决定冲突的点（必须用户拍板）
1. **规范主色是「中蓝色 #3D719F」（RGB 61/113/159）**，而用户先前选的是「沿用现有蓝绿 `#63c9c1`/`#17786f`」。
   两者不一致 → 当前站点用的是蓝绿。**待用户决定是否改回官方规范色。**
2. 规范里有正式 slogan「**简单生活·纯净相伴**」（可替换 `company.slogan` 的 TODO）。
   注意「纯净」二字与「饮用天然水」口径的潜在冲突，需用户确认。
3. Logo 主标**白底不透明**：直接用会出现白方块。接入前需抠底成透明 PNG，另做深底版
   （建议用金色艺术字抠底，或改用文字标）。

### 🚫 物料库里的合规雷区（已排除，不可使用）
- `水邻居桶装水促销封面图.png` / `促销活动长图.png`：画面写「**打垮廊坊 怡宝价格天花板！32桶仅460元**」
  → **点名竞品（怡宝）+ 价格对比**，违反既定合规红线（不点名竞品、不做原价→现价对比）。
- `【水邻居18.9L】PREMIUM PURIFIED WATER`：英文写 **PURIFIED WATER（纯净水）**，与本品类
  「饮用天然水」冲突 → 不可直接使用（可裁掉英文部分后再评估）。

## 本地验证结果（已通过）
- `npm ci` 成功（641 包，19 分钟；同时验证 package-lock 正确）
- `npm run lint` RC=0，**0 warning 0 error**；`npm run build` RC=0，11 条静态产物
- `scripts/verify_out.py`（新增）全绿：必现 5 项命中、禁现 5 项均为 0（**`矿泉水` 0 处**）、
  5 个路由齐备、sitemap 5 条全部指向 `linju.meihaowater.site`；产物 3.03 MB
- 占位基线：图片占位 16 处、logo 占位 15 处
- 本地提交：`57a9590`（内容+配置）、`4684bda`（.gitattributes 锁 LF）

## 未完成（阻塞在用户）
- **GitHub 仓库未建**：本机无 `gh` CLI，需用户在网页新建 public 仓库 `shuineighbor-water`，我再 add remote + push
- **DNS 未解析**：服务器上实测 `getent hosts linju.meihaowater.site` 退出码 2（不存在），
  对照 `meihaowater.site` → `49.233.87.42` 正常 → 需用户添加 A 记录后才能加 Caddy 块（否则 ACME 签发失败）
- 首次素材接入（logo 抠底 + 反白版）与配色决策

